//updates the original value 
//Removes the last element from the array



let orignial = [7,6,6,1,9,3,6,1,1,8]

let remove = orignial.pop();
console.log(orignial); //  returns the updated array
console.log(remove); // returns the poped element
